const CONFIG = {
    // List of active bots - Add your bot usernames here
    BOTS: [
        "your_main_bot",
        "your_backup_bot"
    ],
    // Default bot to use if no others are available
    DEFAULT_BOT: "your_main_bot",
    // Delay before redirect (in milliseconds)
    REDIRECT_DELAY: 1000,
    // Debug mode - set to true to see console logs
    DEBUG: false
};
